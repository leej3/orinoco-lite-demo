contract_version: 1

classes:
  template_owned:
    behavior: copier-three-way-update
    paths:
      - README.md
      - .gitignore
      - .copier-answers.yml
      - pixi.toml
      - .orinoco-lite/template-ownership.yml
      - .agents/skills/**
      - .github/workflows/pages.yml
      - .github/workflows/shacl-vue-proposal.yml
      - .github/workflows/update-orinoco.yml
      - .github/workflows/validate.yml
      - docs/ownership.md
      - docs/updating.md
      - docs/getting-started.md
      - docs/custom-domain.md
      - .orinoco-lite/README.md
      - .orinoco-lite/tools/**
      - .orinoco-lite/source-adapters/**
  initialized_site_owned:
    behavior: create-once-never-overwrite
    paths:
      - orinoco.yaml
      - .orinoco-lite/provenance/**
      - .github/workflows/curation-review.yml
      - metadata/records/**
      - metadata/overlays/annotations/**
      - custom/editorial/**
      - custom/assets/**
      - site/**
      - source-adapters/**
  engine_lock:
    behavior: updater-structured-merge
    paths:
      - orinoco.lock
      - pixi.lock
  extensions:
    behavior: site-owned-stable-hook
    paths:
      - extensions/**
  consumer_tests:
    behavior: site-owned-acceptance
    paths:
      - .orinoco-lite/tests/__init__.py
      - .orinoco-lite/tests/contract/**
      - .orinoco-lite/tests/browser/**
      - .orinoco-lite/tests/source-adapters/**
      - .orinoco-lite/tests/offline/**
      - package.json
      - package-lock.json
      - playwright.config.*
  site_policy:
    behavior: site-owned-policy
    paths:
      - LICENSE*
      - CITATION*
      - CONTRIBUTING*
      - CODE_OF_CONDUCT*
  generated:
    behavior: ignored-runtime-output
    paths:
      - generated/**

semantic_migrations:
  policy: explicit-ledger-and-human-review
  default: forbidden
